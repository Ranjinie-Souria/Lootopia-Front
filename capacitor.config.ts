import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lootopia.app',
  appName: 'lootopia-front',
  webDir: 'dist/lootopia_front/browser',
};

export default config;
